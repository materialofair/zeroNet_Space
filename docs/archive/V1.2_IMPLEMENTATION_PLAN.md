# 零网络空间 V1.2 详细实现计划

**文档版本**: 1.0  
**创建日期**: 2025-11-15  
**计划周期**: 2-3周（12-15个工作日）

---

## 📋 目录

1. [项目总览](#项目总览)
2. [Phase 1: 核心安全功能](#phase-1-核心安全功能)
3. [Phase 2: 隐私增强功能](#phase-2-隐私增强功能)
4. [Phase 3: 体验优化](#phase-3-体验优化)
5. [测试计划](#测试计划)
6. [技术债务和优化](#技术债务和优化)

---

## 项目总览

### 🎯 V1.2 核心目标
实现四大核心功能，打造真正的"隐私增强版"：
1. ✨ 伪装计算器界面（最大亮点）
2. 🔒 无网络验证界面（建立信任）
3. 📝 隐藏空间 + 加密笔记（差异化）
4. 🎨 深色模式 + 批量操作（体验优化）

### 📊 开发资源分配

| Phase | 功能模块 | 工作日 | 占比 |
|-------|---------|-------|------|
| Phase 1 | 核心安全功能 | 3-4天 | 30% |
| Phase 2 | 隐私增强功能 | 4-5天 | 40% |
| Phase 3 | 体验优化 | 2-3天 | 20% |
| 测试调优 | 全功能测试 | 3-4天 | 10% |

---

## Phase 1: 核心安全功能

### 📅 时间安排: Day 1-4

---

### 🔧 Task 1.1: 无网络验证界面实现

**优先级**: P0 (最高)  
**工作量**: 0.5-1天  
**负责模块**: 安全验证

#### 📝 实现步骤

**Step 1: 完善 NetworkVerificationView**

文件: `ZeroNet-Space/Views/Security/NetworkVerificationView.swift`

```swift
// 需要实现的核心功能
struct NetworkVerificationView: View {
    var body: some View {
        ScrollView {
            VStack(spacing: 30) {
                // 1. 标题区域
                headerSection
                
                // 2. 网络权限状态展示
                networkPermissionStatus
                
                // 3. 代码级验证说明
                codeVerificationSection
                
                // 4. 隐私承诺
                privacyCommitmentSection
                
                // 5. 技术细节（可折叠）
                technicalDetailsSection
                
                // 6. 确认按钮
                confirmButton
            }
        }
    }
}
```

**实现要点**:
1. **网络权限检测**:
   ```swift
   // 读取 Info.plist 验证无网络权限
   func checkNetworkPermissions() -> Bool {
       guard let infoPlist = Bundle.main.infoDictionary else {
           return false
       }
       // 检查是否声明了网络权限
       return infoPlist["NSAppTransportSecurity"] == nil
   }
   ```

2. **视觉设计**:
   - 顶部: 盾牌图标 + "零网络验证" 标题
   - 中部: 绿色勾选 ✅ "无网络权限" + 解释文案
   - 底部: "代码级保证" + "隐私承诺"

3. **文案内容**:
   ```
   ✅ 网络权限: 已禁用
   ✅ 第三方SDK: 无
   ✅ 数据上传: 不可能
   ✅ 追踪代码: 不存在
   
   技术保证:
   • Info.plist 无网络权限声明
   • 代码中无 URLSession / Alamofire 调用
   • 无任何云服务 SDK
   • 所有数据仅存储在您的设备
   ```

**Step 2: 集成到设置页面**

文件: `ZeroNet-Space/Views/Settings/SettingsView.swift`

```swift
// 添加入口
Section("安全与隐私") {
    NavigationLink(destination: NetworkVerificationView()) {
        Label("网络验证", systemImage: "shield.checkerboard")
    }
}
```

#### ✅ 验收标准
- [ ] 显示网络权限状态（禁用）
- [ ] 展示技术验证说明
- [ ] 文案清晰易懂
- [ ] 界面美观专业
- [ ] 从设置页面可访问

---

### 🔧 Task 1.2: 伪装计算器界面

**优先级**: P0 (最高)  
**工作量**: 2-3天  
**负责模块**: 伪装模式

#### 📂 新建文件结构

```
ZeroNet-Space/
├── Views/
│   └── Disguise/
│       ├── CalculatorView.swift          (主计算器界面)
│       ├── CalculatorButtonView.swift    (计算器按钮组件)
│       └── DisguiseSettingsView.swift    (伪装设置页面)
├── ViewModels/
│   └── CalculatorViewModel.swift         (计算器逻辑)
├── Models/
│   └── CalculatorState.swift             (计算器状态)
```

#### 📝 详细实现

**Step 1: 数据模型**

文件: `Models/CalculatorState.swift`

```swift
import Foundation

/// 计算器状态模型
struct CalculatorState {
    var displayValue: String = "0"
    var previousValue: Double = 0
    var currentOperation: Operation? = nil
    var shouldResetDisplay: Bool = false
    var inputHistory: [String] = []  // 用于密码检测
    
    enum Operation: String {
        case add = "+"
        case subtract = "-"
        case multiply = "×"
        case divide = "÷"
        
        func calculate(_ a: Double, _ b: Double) -> Double {
            switch self {
            case .add: return a + b
            case .subtract: return a - b
            case .multiply: return a * b
            case .divide: return b != 0 ? a / b : 0
            }
        }
    }
}
```

**Step 2: ViewModel**

文件: `ViewModels/CalculatorViewModel.swift`

```swift
import Foundation
import SwiftUI

class CalculatorViewModel: ObservableObject {
    @Published var state = CalculatorState()
    @Published var shouldUnlock: Bool = false
    
    // 密码序列配置（从AppSettings读取）
    private let passwordSequence: String
    private let maxHistoryLength = 20
    
    init() {
        // 从设置读取密码序列（默认为主密码）
        self.passwordSequence = KeychainService.shared.getPasswordSequence() ?? "1234"
    }
    
    // MARK: - 计算器逻辑
    
    func numberPressed(_ number: Int) {
        let numString = String(number)
        
        if state.shouldResetDisplay {
            state.displayValue = numString
            state.shouldResetDisplay = false
        } else {
            state.displayValue = state.displayValue == "0" ? numString : state.displayValue + numString
        }
        
        addToHistory(numString)
    }
    
    func decimalPressed() {
        if !state.displayValue.contains(".") {
            state.displayValue += "."
        }
        addToHistory(".")
    }
    
    func operationPressed(_ operation: CalculatorState.Operation) {
        if let currentOp = state.currentOperation {
            calculateResult()
        } else {
            state.previousValue = Double(state.displayValue) ?? 0
        }
        
        state.currentOperation = operation
        state.shouldResetDisplay = true
        addToHistory(operation.rawValue)
    }
    
    func equalsPressed() {
        calculateResult()
        state.currentOperation = nil
        state.shouldResetDisplay = true
        
        // 关键：检查密码序列
        addToHistory("=")
        checkPasswordSequence()
    }
    
    func clearPressed() {
        state = CalculatorState()
    }
    
    private func calculateResult() {
        guard let operation = state.currentOperation,
              let currentValue = Double(state.displayValue) else {
            return
        }
        
        let result = operation.calculate(state.previousValue, currentValue)
        state.displayValue = formatNumber(result)
        state.previousValue = result
    }
    
    private func formatNumber(_ number: Double) -> String {
        if number.truncatingRemainder(dividingBy: 1) == 0 {
            return String(Int(number))
        }
        return String(format: "%.8f", number).trimmingCharacters(in: CharacterSet(charactersIn: "0")).trimmingCharacters(in: CharacterSet(charactersIn: "."))
    }
    
    // MARK: - 密码检测
    
    private func addToHistory(_ input: String) {
        state.inputHistory.append(input)
        if state.inputHistory.count > maxHistoryLength {
            state.inputHistory.removeFirst()
        }
    }
    
    private func checkPasswordSequence() {
        let historyString = state.inputHistory.joined()
        
        // 检查是否包含密码序列
        if historyString.contains(passwordSequence + "=") {
            print("🔓 密码序列匹配，准备解锁")
            shouldUnlock = true
            
            // 清空历史（防止再次触发）
            state.inputHistory.removeAll()
            
            // 延迟解锁（模拟自然计算结果显示）
            DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                AuthenticationViewModel.shared.unlockFromDisguise()
            }
        }
    }
}
```

**Step 3: 计算器UI**

文件: `Views/Disguise/CalculatorView.swift`

```swift
import SwiftUI

struct CalculatorView: View {
    @StateObject private var viewModel = CalculatorViewModel()
    @EnvironmentObject private var authViewModel: AuthenticationViewModel
    
    let buttons: [[CalculatorButton]] = [
        [.clear, .negate, .percent, .divide],
        [.seven, .eight, .nine, .multiply],
        [.four, .five, .six, .subtract],
        [.one, .two, .three, .add],
        [.zero, .decimal, .equals]
    ]
    
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            
            VStack(spacing: 12) {
                Spacer()
                
                // 显示屏
                displayView
                
                // 按钮网格
                ForEach(buttons, id: \.self) { row in
                    HStack(spacing: 12) {
                        ForEach(row, id: \.self) { button in
                            CalculatorButtonView(button: button, viewModel: viewModel)
                        }
                    }
                }
            }
            .padding()
        }
        .onChange(of: viewModel.shouldUnlock) { newValue in
            if newValue {
                authViewModel.isAuthenticated = true
            }
        }
    }
    
    private var displayView: some View {
        Text(viewModel.state.displayValue)
            .font(.system(size: 80, weight: .light))
            .foregroundColor(.white)
            .frame(maxWidth: .infinity, alignment: .trailing)
            .padding(.horizontal)
            .minimumScaleFactor(0.5)
            .lineLimit(1)
    }
}

// MARK: - 计算器按钮枚举

enum CalculatorButton: Hashable {
    case zero, one, two, three, four, five, six, seven, eight, nine
    case add, subtract, multiply, divide
    case equals, clear, negate, percent, decimal
    
    var title: String {
        switch self {
        case .zero: return "0"
        case .one: return "1"
        case .two: return "2"
        case .three: return "3"
        case .four: return "4"
        case .five: return "5"
        case .six: return "6"
        case .seven: return "7"
        case .eight: return "8"
        case .nine: return "9"
        case .add: return "+"
        case .subtract: return "-"
        case .multiply: return "×"
        case .divide: return "÷"
        case .equals: return "="
        case .clear: return "AC"
        case .negate: return "+/-"
        case .percent: return "%"
        case .decimal: return "."
        }
    }
    
    var backgroundColor: Color {
        switch self {
        case .add, .subtract, .multiply, .divide, .equals:
            return Color.orange
        case .clear, .negate, .percent:
            return Color.gray.opacity(0.5)
        default:
            return Color.gray.opacity(0.3)
        }
    }
}
```

**Step 4: 计算器按钮组件**

文件: `Views/Disguise/CalculatorButtonView.swift`

```swift
import SwiftUI

struct CalculatorButtonView: View {
    let button: CalculatorButton
    @ObservedObject var viewModel: CalculatorViewModel
    
    var body: some View {
        Button(action: {
            handleButtonPress()
        }) {
            Text(button.title)
                .font(.system(size: 32, weight: .medium))
                .foregroundColor(.white)
                .frame(
                    width: buttonWidth(),
                    height: 80
                )
                .background(button.backgroundColor)
                .cornerRadius(40)
        }
    }
    
    private func buttonWidth() -> CGFloat {
        // 0 按钮占两列
        if button == .zero {
            return (UIScreen.main.bounds.width - 5 * 12) / 4 * 2 + 12
        }
        return (UIScreen.main.bounds.width - 5 * 12) / 4
    }
    
    private func handleButtonPress() {
        switch button {
        case .zero: viewModel.numberPressed(0)
        case .one: viewModel.numberPressed(1)
        case .two: viewModel.numberPressed(2)
        case .three: viewModel.numberPressed(3)
        case .four: viewModel.numberPressed(4)
        case .five: viewModel.numberPressed(5)
        case .six: viewModel.numberPressed(6)
        case .seven: viewModel.numberPressed(7)
        case .eight: viewModel.numberPressed(8)
        case .nine: viewModel.numberPressed(9)
        case .decimal: viewModel.decimalPressed()
        case .add: viewModel.operationPressed(.add)
        case .subtract: viewModel.operationPressed(.subtract)
        case .multiply: viewModel.operationPressed(.multiply)
        case .divide: viewModel.operationPressed(.divide)
        case .equals: viewModel.equalsPressed()
        case .clear: viewModel.clearPressed()
        case .negate, .percent: break // 暂不实现
        }
    }
}
```

**Step 5: 伪装模式设置**

文件: `Views/Disguise/DisguiseSettingsView.swift`

```swift
import SwiftUI

struct DisguiseSettingsView: View {
    @AppStorage(AppConstants.UserDefaultsKeys.disguiseModeEnabled) 
    private var disguiseModeEnabled: Bool = false
    
    @AppStorage(AppConstants.UserDefaultsKeys.disguisePasswordSequence) 
    private var passwordSequence: String = ""
    
    @State private var showPasswordInput = false
    
    var body: some View {
        Form {
            Section {
                Toggle("启用伪装模式", isOn: $disguiseModeEnabled)
            } header: {
                Text("伪装计算器")
            } footer: {
                Text("启用后，应用启动时将显示计算器界面而非登录界面")
            }
            
            if disguiseModeEnabled {
                Section {
                    HStack {
                        Text("密码序列")
                        Spacer()
                        Text(passwordSequence.isEmpty ? "使用主密码" : "已设置")
                            .foregroundColor(.secondary)
                    }
                    .onTapGesture {
                        showPasswordInput = true
                    }
                } header: {
                    Text("解锁密码")
                } footer: {
                    Text("在计算器中输入此数字序列后按 = 号即可解锁应用\n示例: 输入 1234 再按 =")
                }
                
                Section {
                    Text("• 计算器完全可用，可进行正常计算")
                    Text("• 密码序列不会显示在计算结果中")
                    Text("• 建议使用 4-6 位数字作为密码序列")
                    Text("• 密码错误时不会有任何提示")
                }
                .font(.caption)
                .foregroundColor(.secondary)
            }
        }
        .navigationTitle("伪装设置")
        .sheet(isPresented: $showPasswordInput) {
            PasswordSequenceInputView(passwordSequence: $passwordSequence)
        }
    }
}

// 密码序列输入视图
struct PasswordSequenceInputView: View {
    @Binding var passwordSequence: String
    @Environment(\.dismiss) var dismiss
    
    var body: some View {
        NavigationStack {
            Form {
                Section {
                    TextField("输入数字序列", text: $passwordSequence)
                        .keyboardType(.numberPad)
                } footer: {
                    Text("建议使用 4-6 位数字，留空则使用主密码")
                }
            }
            .navigationTitle("设置密码序列")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .confirmationAction) {
                    Button("完成") {
                        dismiss()
                    }
                }
                ToolbarItem(placement: .cancellationAction) {
                    Button("取消") {
                        dismiss()
                    }
                }
            }
        }
    }
}
```

**Step 6: 修改启动逻辑**

文件: `Private_AlbumApp.swift`

```swift
// 修改 body
var body: some Scene {
    WindowGroup {
        ZStack {
            Group {
                if authViewModel.isAuthenticated {
                    ContentView()
                        .environmentObject(authViewModel)
                } else {
                    // 新增：检查是否启用伪装模式
                    if AppSettings.shared.disguiseModeEnabled {
                        CalculatorView()
                            .environmentObject(authViewModel)
                    } else if authViewModel.isPasswordSet {
                        LoginView()
                            .environmentObject(authViewModel)
                    } else {
                        SetupPasswordView()
                            .environmentObject(authViewModel)
                    }
                }
            }
            
            if showLaunchScreen {
                LaunchScreenView()
                    .transition(.opacity)
                    .zIndex(1)
            }
        }
        .task {
            try? await Task.sleep(nanoseconds: 1_500_000_000)
            withAnimation(.easeOut(duration: 0.5)) {
                showLaunchScreen = false
            }
        }
    }
    .modelContainer(sharedModelContainer)
}
```

**Step 7: 更新 AppSettings**

文件: `Models/AppSettings.swift`

```swift
// 添加伪装模式相关属性
@Published var disguiseModeEnabled: Bool {
    didSet {
        defaults.set(disguiseModeEnabled, forKey: AppConstants.UserDefaultsKeys.disguiseModeEnabled)
    }
}

// 在 init 中初始化
self.disguiseModeEnabled = defaults.bool(forKey: AppConstants.UserDefaultsKeys.disguiseModeEnabled)
```

**Step 8: 更新 AppConstants**

文件: `App/AppConstants.swift`

```swift
// 在 UserDefaultsKeys 中添加
static let disguiseModeEnabled = "disguiseModeEnabled"
static let disguisePasswordSequence = "disguisePasswordSequence"
```

#### ✅ 验收标准
- [ ] 计算器完全可用（四则运算）
- [ ] 密码序列输入后正确解锁
- [ ] 伪装模式可在设置中开关
- [ ] 密码序列可自定义
- [ ] 界面与系统计算器相似
- [ ] 无任何"伪装"的视觉线索
- [ ] 解锁动画流畅自然

---

## Phase 2: 隐私增强功能

### 📅 时间安排: Day 5-9

---

### 🔧 Task 2.1: 隐藏空间核心系统

**优先级**: P1  
**工作量**: 3-4天  
**负责模块**: 隐藏空间 + 加密笔记

#### 📂 新建文件结构

```
ZeroNet-Space/
├── Models/
│   └── SecretNote.swift                  (笔记数据模型)
├── Views/
│   └── SecretSpace/
│       ├── SecretSpaceView.swift         (隐藏空间主界面)
│       ├── SecretNoteListView.swift      (笔记列表)
│       ├── SecretNoteEditorView.swift    (笔记编辑器)
│       └── SecretPasswordSetupView.swift (第二密码设置)
├── ViewModels/
│   └── SecretSpaceViewModel.swift        (隐藏空间逻辑)
├── Services/
│   └── SecretSpaceService.swift          (第二密码服务)
```

#### 📝 详细实现

**Step 1: 数据模型**

文件: `Models/SecretNote.swift`

```swift
import Foundation
import SwiftData

@Model
final class SecretNote {
    var id: UUID
    var title: String
    var content: String
    var createdAt: Date
    var updatedAt: Date
    var isFavorite: Bool
    
    init(title: String, content: String) {
        self.id = UUID()
        self.title = title
        self.content = content
        self.createdAt = Date()
        self.updatedAt = Date()
        self.isFavorite = false
    }
    
    // 更新笔记
    func update(title: String, content: String) {
        self.title = title
        self.content = content
        self.updatedAt = Date()
    }
}
```

**Step 2: 第二密码服务**

文件: `Services/SecretSpaceService.swift`

```swift
import Foundation

class SecretSpaceService {
    static let shared = SecretSpaceService()
    
    private let keychainService = KeychainService.shared
    private let secondPasswordKey = "com.zeronetspace.secondPassword"
    private let secondPasswordSaltKey = "com.zeronetspace.secondPasswordSalt"
    
    // MARK: - 第二密码管理
    
    /// 是否已设置第二密码
    func isSecondPasswordSet() -> Bool {
        return keychainService.keyExists(key: secondPasswordKey)
    }
    
    /// 设置第二密码
    func setSecondPassword(_ password: String) -> Bool {
        let salt = generateSalt()
        let hashedPassword = hashPassword(password, salt: salt)
        
        // 保存哈希密码和盐值
        let passwordSaved = keychainService.save(key: secondPasswordKey, value: hashedPassword)
        let saltSaved = keychainService.save(key: secondPasswordSaltKey, value: salt)
        
        return passwordSaved && saltSaved
    }
    
    /// 验证第二密码
    func verifySecondPassword(_ password: String) -> Bool {
        guard let savedHash = keychainService.load(key: secondPasswordKey),
              let salt = keychainService.load(key: secondPasswordSaltKey) else {
            return false
        }
        
        let inputHash = hashPassword(password, salt: salt)
        return inputHash == savedHash
    }
    
    /// 删除第二密码
    func deleteSecondPassword() -> Bool {
        let passwordDeleted = keychainService.delete(key: secondPasswordKey)
        let saltDeleted = keychainService.delete(key: secondPasswordSaltKey)
        return passwordDeleted && saltDeleted
    }
    
    // MARK: - 加密辅助
    
    private func hashPassword(_ password: String, salt: String) -> String {
        let combined = password + salt
        return combined.data(using: .utf8)?.base64EncodedString() ?? ""
    }
    
    private func generateSalt() -> String {
        return UUID().uuidString
    }
}
```

**Step 3: ViewModel**

文件: `ViewModels/SecretSpaceViewModel.swift`

```swift
import Foundation
import SwiftUI
import SwiftData

class SecretSpaceViewModel: ObservableObject {
    @Published var isUnlocked: Bool = false
    @Published var notes: [SecretNote] = []
    @Published var showPasswordSetup: Bool = false
    @Published var errorMessage: String?
    
    private let service = SecretSpaceService.shared
    
    // MARK: - 认证
    
    func checkSecondPasswordStatus() -> Bool {
        return service.isSecondPasswordSet()
    }
    
    func setupSecondPassword(_ password: String) -> Bool {
        let success = service.setSecondPassword(password)
        if success {
            isUnlocked = true
        }
        return success
    }
    
    func unlock(with password: String) -> Bool {
        if service.verifySecondPassword(password) {
            isUnlocked = true
            errorMessage = nil
            return true
        } else {
            errorMessage = "密码错误"
            return false
        }
    }
    
    func lock() {
        isUnlocked = false
    }
    
    // MARK: - 笔记操作
    
    func loadNotes(context: ModelContext) {
        let descriptor = FetchDescriptor<SecretNote>(
            sortBy: [SortDescriptor(\.updatedAt, order: .reverse)]
        )
        
        do {
            notes = try context.fetch(descriptor)
        } catch {
            print("❌ 加载隐藏笔记失败: \(error)")
        }
    }
    
    func addNote(title: String, content: String, context: ModelContext) {
        let note = SecretNote(title: title, content: content)
        context.insert(note)
        
        do {
            try context.save()
            loadNotes(context: context)
        } catch {
            print("❌ 保存笔记失败: \(error)")
        }
    }
    
    func deleteNote(_ note: SecretNote, context: ModelContext) {
        context.delete(note)
        
        do {
            try context.save()
            loadNotes(context: context)
        } catch {
            print("❌ 删除笔记失败: \(error)")
        }
    }
}
```

**Step 4: 隐藏空间主界面**

文件: `Views/SecretSpace/SecretSpaceView.swift`

```swift
import SwiftUI
import SwiftData

struct SecretSpaceView: View {
    @Environment(\.modelContext) private var modelContext
    @StateObject private var viewModel = SecretSpaceViewModel()
    @State private var password = ""
    
    var body: some View {
        Group {
            if !viewModel.checkSecondPasswordStatus() {
                // 未设置第二密码 - 引导设置
                SecretPasswordSetupView(viewModel: viewModel)
            } else if viewModel.isUnlocked {
                // 已解锁 - 显示笔记列表
                SecretNoteListView(viewModel: viewModel)
            } else {
                // 需要验证 - 显示密码输入
                passwordInputView
            }
        }
    }
    
    private var passwordInputView: some View {
        VStack(spacing: 30) {
            Image(systemName: "lock.shield")
                .font(.system(size: 60))
                .foregroundColor(.purple)
            
            Text("隐藏空间")
                .font(.title)
                .fontWeight(.bold)
            
            VStack(spacing: 15) {
                SecureField("输入第二密码", text: $password)
                    .textFieldStyle(.roundedBorder)
                    .padding(.horizontal)
                
                if let error = viewModel.errorMessage {
                    Text(error)
                        .foregroundColor(.red)
                        .font(.caption)
                }
                
                Button("解锁") {
                    _ = viewModel.unlock(with: password)
                    password = ""
                }
                .buttonStyle(.borderedProminent)
                .disabled(password.isEmpty)
            }
        }
        .padding()
    }
}
```

**Step 5: 笔记列表**

文件: `Views/SecretSpace/SecretNoteListView.swift`

```swift
import SwiftUI
import SwiftData

struct SecretNoteListView: View {
    @Environment(\.modelContext) private var modelContext
    @ObservedObject var viewModel: SecretSpaceViewModel
    @State private var showEditor = false
    
    var body: some View {
        NavigationStack {
            List {
                ForEach(viewModel.notes) { note in
                    NavigationLink(destination: SecretNoteEditorView(note: note, viewModel: viewModel)) {
                        VStack(alignment: .leading, spacing: 4) {
                            Text(note.title)
                                .font(.headline)
                            Text(note.content)
                                .font(.caption)
                                .foregroundColor(.secondary)
                                .lineLimit(2)
                            Text(note.updatedAt, style: .relative)
                                .font(.caption2)
                                .foregroundColor(.secondary)
                        }
                    }
                }
                .onDelete(perform: deleteNotes)
            }
            .navigationTitle("私密笔记")
            .toolbar {
                ToolbarItem(placement: .primaryAction) {
                    Button(action: { showEditor = true }) {
                        Image(systemName: "plus")
                    }
                }
                ToolbarItem(placement: .navigationBarLeading) {
                    Button("锁定") {
                        viewModel.lock()
                    }
                }
            }
            .sheet(isPresented: $showEditor) {
                SecretNoteEditorView(note: nil, viewModel: viewModel)
            }
            .onAppear {
                viewModel.loadNotes(context: modelContext)
            }
        }
    }
    
    private func deleteNotes(at offsets: IndexSet) {
        offsets.forEach { index in
            viewModel.deleteNote(viewModel.notes[index], context: modelContext)
        }
    }
}
```

**Step 6: 笔记编辑器**

文件: `Views/SecretSpace/SecretNoteEditorView.swift`

```swift
import SwiftUI
import SwiftData

struct SecretNoteEditorView: View {
    @Environment(\.modelContext) private var modelContext
    @Environment(\.dismiss) private var dismiss
    
    let note: SecretNote?
    @ObservedObject var viewModel: SecretSpaceViewModel
    
    @State private var title: String = ""
    @State private var content: String = ""
    
    var body: some View {
        NavigationStack {
            VStack(spacing: 0) {
                TextField("标题", text: $title)
                    .font(.title2)
                    .padding()
                
                Divider()
                
                TextEditor(text: $content)
                    .padding()
            }
            .navigationTitle(note == nil ? "新建笔记" : "编辑笔记")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .confirmationAction) {
                    Button("保存") {
                        saveNote()
                    }
                    .disabled(title.isEmpty)
                }
                ToolbarItem(placement: .cancellationAction) {
                    Button("取消") {
                        dismiss()
                    }
                }
            }
        }
        .onAppear {
            if let note = note {
                title = note.title
                content = note.content
            }
        }
    }
    
    private func saveNote() {
        if let note = note {
            note.update(title: title, content: content)
        } else {
            viewModel.addNote(title: title, content: content, context: modelContext)
        }
        dismiss()
    }
}
```

**Step 7: 第二密码设置**

文件: `Views/SecretSpace/SecretPasswordSetupView.swift`

```swift
import SwiftUI

struct SecretPasswordSetupView: View {
    @ObservedObject var viewModel: SecretSpaceViewModel
    @Environment(\.dismiss) private var dismiss
    
    @State private var password = ""
    @State private var confirmPassword = ""
    @State private var showError = false
    @State private var errorMessage = ""
    
    var body: some View {
        VStack(spacing: 30) {
            Image(systemName: "key.fill")
                .font(.system(size: 60))
                .foregroundColor(.purple)
            
            VStack(spacing: 8) {
                Text("设置第二密码")
                    .font(.title)
                    .fontWeight(.bold)
                
                Text("为隐藏空间设置独立密码")
                    .font(.subheadline)
                    .foregroundColor(.secondary)
            }
            
            VStack(spacing: 15) {
                SecureField("输入密码", text: $password)
                    .textFieldStyle(.roundedBorder)
                
                SecureField("确认密码", text: $confirmPassword)
                    .textFieldStyle(.roundedBorder)
                
                if showError {
                    Text(errorMessage)
                        .foregroundColor(.red)
                        .font(.caption)
                }
            }
            .padding(.horizontal)
            
            Button("创建隐藏空间") {
                setupPassword()
            }
            .buttonStyle(.borderedProminent)
            .disabled(password.isEmpty || confirmPassword.isEmpty)
            
            Button("暂不设置") {
                dismiss()
            }
            .foregroundColor(.secondary)
        }
        .padding()
    }
    
    private func setupPassword() {
        guard password == confirmPassword else {
            errorMessage = "两次密码不一致"
            showError = true
            return
        }
        
        guard password.count >= 4 else {
            errorMessage = "密码至少需要4位"
            showError = true
            return
        }
        
        if viewModel.setupSecondPassword(password) {
            dismiss()
        } else {
            errorMessage = "设置失败，请重试"
            showError = true
        }
    }
}
```

**Step 8: 集成到设置页面**

文件: `Views/Settings/SettingsView.swift`

```swift
// 添加隐藏空间入口
Section("隐私功能") {
    NavigationLink(destination: SecretSpaceView()) {
        Label("隐藏空间", systemImage: "eye.slash")
    }
    
    NavigationLink(destination: DisguiseSettingsView()) {
        Label("伪装模式", systemImage: "theatermasks")
    }
}
```

**Step 9: 更新 SwiftData Schema**

文件: `Private_AlbumApp.swift`

```swift
// 在 sharedModelContainer 中添加 SecretNote
var sharedModelContainer: ModelContainer = {
    let schema = Schema([
        MediaItem.self,
        Folder.self,
        Tag.self,
        SecretNote.self,  // 新增
    ])
    // ...
}()
```

#### ✅ 验收标准
- [ ] 第二密码独立设置和验证
- [ ] 笔记创建、编辑、删除功能
- [ ] 笔记加密存储（SwiftData）
- [ ] 未设置第二密码时隐藏入口
- [ ] 锁定后需要重新输入密码
- [ ] 界面美观易用

---

## Phase 3: 体验优化

### 📅 时间安排: Day 10-12

---

### 🔧 Task 3.1: 文件批量操作增强

**优先级**: P2  
**工作量**: 1天  

#### 实现要点

**Step 1: 批量移动到文件夹**

文件: `Views/Gallery/GalleryView.swift`

```swift
// 添加批量移动功能
.toolbar {
    if selectionMode {
        ToolbarItem(placement: .bottomBar) {
            Button("移动") {
                showBatchMoveSheet = true
            }
            .disabled(selectedItems.isEmpty)
        }
    }
}
.sheet(isPresented: $showBatchMoveSheet) {
    BatchFolderSelectionView(
        selectedItems: selectedItems,
        onComplete: {
            selectionMode = false
            selectedItems.removeAll()
        }
    )
}
```

**Step 2: 批量导出**

```swift
ToolbarItem(placement: .bottomBar) {
    Button("导出") {
        exportSelectedItems()
    }
    .disabled(selectedItems.isEmpty)
}

func exportSelectedItems() {
    // 使用 UIActivityViewController
    let urls = selectedItems.compactMap { item in
        // 解密并导出
    }
    
    let activityVC = UIActivityViewController(
        activityItems: urls,
        applicationActivities: nil
    )
    
    // Present activity controller
}
```

---

### 🔧 Task 3.2: 深色模式适配

**优先级**: P2  
**工作量**: 1-2天  

#### 实现步骤

**Step 1: 定义颜色主题**

新建文件: `App/ColorTheme.swift`

```swift
import SwiftUI

extension Color {
    // 主要颜色
    static let appPrimary = Color("AppPrimary")
    static let appSecondary = Color("AppSecondary")
    static let appBackground = Color("AppBackground")
    static let appCardBackground = Color("AppCardBackground")
    
    // 文本颜色
    static let appTextPrimary = Color("AppTextPrimary")
    static let appTextSecondary = Color("AppTextSecondary")
}
```

**Step 2: 添加 Assets 颜色集**

在 `Assets.xcassets` 中添加颜色集：
- AppPrimary (Light/Dark)
- AppSecondary (Light/Dark)
- AppBackground (Light/Dark)
- AppCardBackground (Light/Dark)
- AppTextPrimary (Light/Dark)
- AppTextSecondary (Light/Dark)

**Step 3: 全局替换硬编码颜色**

批量替换：
- `.blue` → `.appPrimary`
- `.gray` → `.appSecondary`
- `Color(.systemBackground)` → `.appBackground`

**Step 4: 测试所有界面**

在浅色和深色模式下测试：
- 登录界面
- 计算器界面
- 主界面
- 设置界面
- 隐藏空间
- 所有弹窗和 Sheet

---

## 测试计划

### 📅 时间安排: Day 13-15

---

### 🧪 测试用例

#### 1. 伪装计算器测试

| 测试项 | 测试步骤 | 预期结果 |
|-------|---------|---------|
| 计算功能 | 输入 2+2= | 显示 4 |
| 密码解锁 | 输入 1234= | 进入主应用 |
| 错误密码 | 输入 9999= | 显示计算结果，不解锁 |
| 伪装开关 | 关闭伪装模式 | 启动显示登录界面 |

#### 2. 隐藏空间测试

| 测试项 | 测试步骤 | 预期结果 |
|-------|---------|---------|
| 密码设置 | 设置第二密码 | 成功保存到 Keychain |
| 密码验证 | 输入正确密码 | 解锁隐藏空间 |
| 笔记创建 | 创建新笔记 | 成功保存并加密 |
| 笔记编辑 | 修改笔记内容 | 更新时间戳 |
| 笔记删除 | 删除笔记 | 从数据库移除 |
| 锁定功能 | 点击锁定 | 返回密码输入界面 |

#### 3. 安全性测试

| 测试项 | 测试步骤 | 预期结果 |
|-------|---------|---------|
| 网络权限 | 检查 Info.plist | 无网络权限声明 |
| 代码扫描 | 搜索 URLSession | 无网络代码 |
| Keychain 隔离 | 检查密钥存储 | 主密码和第二密码独立 |
| 数据加密 | 检查数据库文件 | 所有内容加密存储 |

#### 4. 性能测试

| 测试项 | 测试步骤 | 预期结果 |
|-------|---------|---------|
| 启动时间 | 冷启动 | < 2秒 |
| 计算器响应 | 快速点击按钮 | 无延迟 |
| 笔记加载 | 加载 100 条笔记 | < 1秒 |
| 内存占用 | 长时间使用 | < 100MB |

---

## 技术债务和优化

### 🔄 可选优化项

1. **计算器高级功能**
   - 科学计算模式
   - 计算历史记录
   - 更复杂的密码检测

2. **隐藏空间增强**
   - Markdown 支持
   - 笔记分类
   - 笔记搜索

3. **批量操作扩展**
   - 批量重命名
   - 批量压缩
   - 批量分享

4. **性能优化**
   - SwiftData 查询优化
   - 图片缓存策略
   - 启动性能优化

---

## 📊 开发进度跟踪

### Week 1: 核心安全功能

- [x] Day 1: 无网络验证界面
- [x] Day 2-3: 伪装计算器 UI
- [x] Day 4: 伪装计算器逻辑 + 测试

### Week 2: 隐私增强功能

- [x] Day 5: 第二密码系统
- [x] Day 6-7: 笔记数据模型 + UI
- [x] Day 8: 隐藏空间集成
- [x] Day 9: 功能测试和调优

### Week 3: 体验优化 + 测试

- [x] Day 10: 批量操作增强
- [x] Day 11-12: 深色模式适配
- [x] Day 13-15: 全面测试 + Bug 修复

---

## ✅ 完成标准

### 功能完整性
- [ ] 所有 V1.2 核心功能实现
- [ ] 所有测试用例通过
- [ ] 无崩溃和严重 Bug

### 代码质量
- [ ] 遵循 Swift 最佳实践
- [ ] 代码注释完整
- [ ] 无硬编码字符串（使用本地化）

### 安全性
- [ ] 零网络代码
- [ ] 数据全部加密
- [ ] Keychain 正确使用

### 用户体验
- [ ] 界面流畅（60fps）
- [ ] 操作符合直觉
- [ ] 深色模式美观

---

## 🚀 发布准备

### App Store 准备

1. **截图更新**
   - 伪装计算器界面
   - 隐藏空间展示
   - 无网络验证界面

2. **功能描述**
   ```
   V1.2 更新：
   ✨ 全新伪装计算器模式，隐私升级
   🔒 隐藏空间 + 加密笔记本
   🛡️ 可视化网络验证界面
   📦 文件批量操作增强
   🌙 深色模式全面支持
   ```

3. **测试准备**
   - TestFlight 内测
   - 收集用户反馈
   - Bug 修复迭代

---

**文档状态**: ✅ 完成  
**准备开始**: Phase 1 实施

