# 零网络空间 V1.2 功能需求细化

**文档版本**: 1.1  
**更新日期**: 2025-11-15  
**更新原因**: 根据用户反馈细化功能需求

---

## 📝 需求细化说明

### 1. ✨ 伪装计算器模式

#### 密码输入规则
- ✅ **支持字符**: 仅支持数字 (0-9) 和小数点 (.)
- ❌ **不支持**: 运算符作为密码（+、-、×、÷ 仅用于计算）
- 📌 **示例密码**: `1234.56`、`9876`、`3.14159`

#### 密码序列检测
```swift
// 密码输入方式
用户输入: 1 2 3 4 . 5 6 =
检测逻辑: 提取数字序列 "1234.56"
匹配密码: 如果 "1234.56" == 设置的密码 → 解锁
```

#### 计算历史
- ❌ **不保留历史记录**
- 每次计算后清空输入历史
- 密码检测后立即清空历史（防止二次触发）

#### 实现调整

**CalculatorViewModel.swift 修改**:
```swift
class CalculatorViewModel: ObservableObject {
    @Published var state = CalculatorState()
    @Published var shouldUnlock: Bool = false
    
    private let passwordSequence: String
    private let maxHistoryLength = 20
    
    // ❌ 移除计算历史显示功能
    // ✅ 仅用于密码检测的临时历史
    
    func numberPressed(_ number: Int) {
        let numString = String(number)
        
        if state.shouldResetDisplay {
            state.displayValue = numString
            state.shouldResetDisplay = false
        } else {
            state.displayValue = state.displayValue == "0" ? numString : state.displayValue + numString
        }
        
        // 仅记录数字用于密码检测
        addToPasswordHistory(numString)
    }
    
    func decimalPressed() {
        if !state.displayValue.contains(".") {
            state.displayValue += "."
            addToPasswordHistory(".")  // 小数点也计入密码
        }
    }
    
    func operationPressed(_ operation: CalculatorState.Operation) {
        if let currentOp = state.currentOperation {
            calculateResult()
        } else {
            state.previousValue = Double(state.displayValue) ?? 0
        }
        
        state.currentOperation = operation
        state.shouldResetDisplay = true
        
        // ❌ 不将运算符计入密码历史
    }
    
    func equalsPressed() {
        calculateResult()
        
        // 关键：检查密码序列（仅数字和小数点）
        checkPasswordSequence()
        
        state.currentOperation = nil
        state.shouldResetDisplay = true
    }
    
    // 密码历史管理（仅数字和小数点）
    private func addToPasswordHistory(_ input: String) {
        state.inputHistory.append(input)
        if state.inputHistory.count > maxHistoryLength {
            state.inputHistory.removeFirst()
        }
    }
    
    private func checkPasswordSequence() {
        // 拼接数字序列（不含运算符）
        let passwordInput = state.inputHistory.joined()
        
        // 检查是否匹配
        if passwordInput == passwordSequence {
            print("🔓 密码匹配: \(passwordInput)")
            shouldUnlock = true
            
            // 立即清空历史
            state.inputHistory.removeAll()
            
            // 延迟解锁
            DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) {
                AuthenticationViewModel.shared.unlockFromDisguise()
            }
        } else {
            // 密码不匹配，清空历史（防止历史累积）
            state.inputHistory.removeAll()
        }
    }
    
    func clearPressed() {
        state = CalculatorState()
        // 清空密码历史
        state.inputHistory.removeAll()
    }
}
```

**CalculatorState.swift 简化**:
```swift
struct CalculatorState {
    var displayValue: String = "0"
    var previousValue: Double = 0
    var currentOperation: Operation? = nil
    var shouldResetDisplay: Bool = false
    var inputHistory: [String] = []  // 仅用于密码检测，不显示
    
    enum Operation: String {
        case add = "+"
        case subtract = "-"
        case multiply = "×"
        case divide = "÷"
        
        func calculate(_ a: Double, _ b: Double) -> Double {
            switch self {
            case .add: return a + b
            case .subtract: return a - b
            case .multiply: return a * b
            case .divide: return b != 0 ? a / b : 0
            }
        }
    }
}
```

---

### 2. 📝 隐藏空间 + Markdown 笔记

#### Markdown 支持
- ✅ **支持 Markdown 语法**
- ❌ **不支持图片附件**
- 📌 **编辑器**: 纯文本 + Markdown 渲染预览

#### 第二密码管理
- ✅ **开关式设计**: 在设置中通过 Toggle 开启/关闭
- ✅ **统一入口**: 所有第二密码操作都在"设置 → 第二密码"中管理

#### 设置界面设计

**SettingsView.swift 修改**:
```swift
Section("隐私功能") {
    // 伪装模式入口
    NavigationLink(destination: DisguiseSettingsView()) {
        Label("伪装模式", systemImage: "theatermasks")
    }
    
    // 第二密码设置（统一入口）
    NavigationLink(destination: SecondPasswordSettingsView()) {
        HStack {
            Label("第二密码", systemImage: "key.fill")
            Spacer()
            if SecretSpaceService.shared.isSecondPasswordSet() {
                Image(systemName: "checkmark.circle.fill")
                    .foregroundColor(.green)
            } else {
                Text("未设置")
                    .foregroundColor(.secondary)
                    .font(.caption)
            }
        }
    }
    
    // 隐藏空间入口（需要第二密码）
    if SecretSpaceService.shared.isSecondPasswordSet() {
        NavigationLink(destination: SecretSpaceView()) {
            Label("隐藏空间", systemImage: "eye.slash")
        }
    }
}
```

#### 第二密码设置页面（新增）

**SecondPasswordSettingsView.swift**:
```swift
import SwiftUI

struct SecondPasswordSettingsView: View {
    @State private var isEnabled = SecretSpaceService.shared.isSecondPasswordSet()
    @State private var showPasswordSetup = false
    @State private var showPasswordChange = false
    @State private var showDeleteConfirmation = false
    
    var body: some View {
        Form {
            Section {
                Toggle("启用第二密码", isOn: $isEnabled)
                    .onChange(of: isEnabled) { newValue in
                        if newValue {
                            // 开启 - 显示密码设置
                            showPasswordSetup = true
                        } else {
                            // 关闭 - 确认删除
                            showDeleteConfirmation = true
                        }
                    }
            } footer: {
                Text("启用后可使用隐藏空间存储私密笔记")
            }
            
            if isEnabled {
                Section("密码管理") {
                    Button("修改第二密码") {
                        showPasswordChange = true
                    }
                    
                    Button("删除第二密码", role: .destructive) {
                        showDeleteConfirmation = true
                    }
                }
                
                Section {
                    Text("• 第二密码独立于主密码")
                    Text("• 删除第二密码将清空隐藏空间所有数据")
                    Text("• 请牢记您的第二密码，无法找回")
                }
                .font(.caption)
                .foregroundColor(.secondary)
            }
        }
        .navigationTitle("第二密码")
        .sheet(isPresented: $showPasswordSetup) {
            SecretPasswordSetupView { success in
                if !success {
                    // 用户取消设置，关闭开关
                    isEnabled = false
                }
            }
        }
        .sheet(isPresented: $showPasswordChange) {
            SecretPasswordChangeView()
        }
        .alert("删除第二密码", isPresented: $showDeleteConfirmation) {
            Button("取消", role: .cancel) {
                isEnabled = true  // 恢复开关状态
            }
            Button("删除", role: .destructive) {
                deleteSecondPassword()
            }
        } message: {
            Text("删除第二密码将清空隐藏空间的所有笔记数据，此操作不可恢复。")
        }
    }
    
    private func deleteSecondPassword() {
        // 删除第二密码和所有笔记
        if SecretSpaceService.shared.deleteSecondPassword() {
            isEnabled = false
            // TODO: 清空所有 SecretNote 数据
        }
    }
}
```

#### Markdown 编辑器实现

**SecretNoteEditorView.swift 修改**:
```swift
import SwiftUI
import SwiftData
import MarkdownUI  // 使用第三方库或自定义渲染

struct SecretNoteEditorView: View {
    @Environment(\.modelContext) private var modelContext
    @Environment(\.dismiss) private var dismiss
    
    let note: SecretNote?
    @ObservedObject var viewModel: SecretSpaceViewModel
    
    @State private var title: String = ""
    @State private var content: String = ""
    @State private var showPreview = false
    
    var body: some View {
        NavigationStack {
            VStack(spacing: 0) {
                // 标题输入
                TextField("标题", text: $title)
                    .font(.title2)
                    .padding()
                
                Divider()
                
                // 编辑/预览切换
                Picker("模式", selection: $showPreview) {
                    Text("编辑").tag(false)
                    Text("预览").tag(true)
                }
                .pickerStyle(.segmented)
                .padding(.horizontal)
                .padding(.vertical, 8)
                
                Divider()
                
                // 内容区域
                if showPreview {
                    // Markdown 预览
                    ScrollView {
                        VStack(alignment: .leading, spacing: 12) {
                            Markdown(content)
                                .padding()
                        }
                    }
                } else {
                    // Markdown 编辑器
                    TextEditor(text: $content)
                        .font(.system(.body, design: .monospaced))
                        .padding()
                }
            }
            .navigationTitle(note == nil ? "新建笔记" : "编辑笔记")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .confirmationAction) {
                    Button("保存") {
                        saveNote()
                    }
                    .disabled(title.isEmpty)
                }
                ToolbarItem(placement: .cancellationAction) {
                    Button("取消") {
                        dismiss()
                    }
                }
            }
        }
        .onAppear {
            if let note = note {
                title = note.title
                content = note.content
            }
        }
    }
    
    private func saveNote() {
        if let note = note {
            note.update(title: title, content: content)
        } else {
            viewModel.addNote(title: title, content: content, context: modelContext)
        }
        dismiss()
    }
}
```

**Markdown 依赖**:

选项1: 使用第三方库
```swift
// Package.swift dependencies
.package(url: "https://github.com/gonzalezreal/swift-markdown-ui", from: "2.0.0")

// Target dependencies
.product(name: "MarkdownUI", package: "swift-markdown-ui")
```

选项2: 简单的自定义渲染（不依赖第三方）
```swift
// 简单的 Markdown 文本显示（不渲染）
struct MarkdownPreview: View {
    let markdown: String
    
    var body: some View {
        Text(markdown)
            .font(.body)
            .multilineTextAlignment(.leading)
    }
}
```

---

### 3. 📦 批量导出功能

#### 导出格式
- ✅ **解密导出**: 导出原始文件（已解密）
- ❌ **不重命名**: 保持原文件名
- 📌 **格式**: 照片 (JPG/PNG)、视频 (MP4/MOV)、文件 (PDF/DOC等)

#### 实现方案

**GalleryViewModel.swift 添加方法**:
```swift
import SwiftUI
import UIKit

extension GalleryViewModel {
    /// 批量导出选中项（解密后的原始文件）
    func exportSelectedItems(_ items: [MediaItem]) async -> [URL] {
        var exportedURLs: [URL] = []
        let tempDirectory = FileManager.default.temporaryDirectory
        
        for item in items {
            // 1. 解密文件
            guard let decryptedData = try? encryptionService.decryptData(
                from: item.encryptedFileURL
            ) else {
                print("❌ 解密失败: \(item.fileName)")
                continue
            }
            
            // 2. 保存到临时目录（保持原文件名）
            let tempURL = tempDirectory.appendingPathComponent(item.fileName)
            
            do {
                try decryptedData.write(to: tempURL)
                exportedURLs.append(tempURL)
                print("✅ 导出成功: \(item.fileName)")
            } catch {
                print("❌ 写入临时文件失败: \(error)")
            }
        }
        
        return exportedURLs
    }
}
```

**GalleryView.swift 添加导出按钮**:
```swift
.toolbar {
    if selectionMode {
        ToolbarItemGroup(placement: .bottomBar) {
            // 移动按钮
            Button {
                showBatchMoveSheet = true
            } label: {
                Label("移动", systemImage: "folder")
            }
            .disabled(selectedItems.isEmpty)
            
            Spacer()
            
            // 导出按钮
            Button {
                Task {
                    await exportSelectedItems()
                }
            } label: {
                Label("导出", systemImage: "square.and.arrow.up")
            }
            .disabled(selectedItems.isEmpty)
            
            Spacer()
            
            // 删除按钮
            Button(role: .destructive) {
                deleteSelectedItems()
            } label: {
                Label("删除", systemImage: "trash")
            }
            .disabled(selectedItems.isEmpty)
        }
    }
}

// 导出方法
private func exportSelectedItems() async {
    isExporting = true
    
    let urls = await viewModel.exportSelectedItems(selectedItems)
    
    guard !urls.isEmpty else {
        isExporting = false
        return
    }
    
    // 使用系统分享表单
    await MainActor.run {
        let activityVC = UIActivityViewController(
            activityItems: urls,
            applicationActivities: nil
        )
        
        // 获取当前窗口
        if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene,
           let rootVC = windowScene.windows.first?.rootViewController {
            
            // iPad 需要设置 popover
            if let popover = activityVC.popoverPresentationController {
                popover.sourceView = rootVC.view
                popover.sourceRect = CGRect(
                    x: rootVC.view.bounds.midX,
                    y: rootVC.view.bounds.midY,
                    width: 0,
                    height: 0
                )
            }
            
            rootVC.present(activityVC, animated: true) {
                isExporting = false
                
                // 导出完成后清理临时文件
                Task {
                    await cleanupTempFiles(urls)
                }
            }
        }
    }
    
    // 退出选择模式
    selectionMode = false
    selectedItems.removeAll()
}

// 清理临时文件
private func cleanupTempFiles(_ urls: [URL]) async {
    for url in urls {
        try? FileManager.default.removeItem(at: url)
    }
}
```

---

## 🔄 功能对比表

| 功能 | 原计划 | 细化后需求 | 变更原因 |
|------|--------|-----------|---------|
| **计算器密码** | 支持所有字符 | 仅支持数字和小数点 | 简化逻辑，提高可靠性 |
| **计算历史** | 可选显示 | 完全不显示 | 减少隐私暴露风险 |
| **Markdown支持** | 未明确 | 支持Markdown语法 | 提升笔记体验 |
| **笔记图片** | 可选支持 | 不支持 | 简化V1.2范围 |
| **第二密码设置** | 独立入口 | 统一在设置中管理 | 更好的用户体验 |
| **批量导出格式** | 可选加密/解密 | 仅支持解密导出 | 符合实际使用场景 |
| **导出重命名** | 可选 | 保持原文件名 | 避免用户困惑 |

---

## 📋 更新的开发检查清单

### Phase 1: 伪装计算器
- [ ] 限制密码字符为数字和小数点
- [ ] 移除计算历史显示功能
- [ ] 密码检测后立即清空历史
- [ ] 测试密码序列: `1234.56`

### Phase 2: 隐藏空间
- [ ] 创建统一的第二密码设置页面
- [ ] 实现开关式启用/禁用
- [ ] 集成 Markdown 编辑和预览
- [ ] 不支持图片附件（纯文本）
- [ ] 删除第二密码时清空所有笔记

### Phase 3: 批量导出
- [ ] 实现解密导出功能
- [ ] 保持原文件名
- [ ] 使用系统分享表单
- [ ] 导出后清理临时文件

---

## ✅ 验收标准更新

### 伪装计算器
- [ ] 密码只能包含 0-9 和 .
- [ ] 计算器界面无历史记录显示
- [ ] 密码: `1234` + `=` 可解锁
- [ ] 密码: `3.14159` + `=` 可解锁
- [ ] 错误密码不显示任何提示

### 隐藏空间
- [ ] 设置中有统一的"第二密码"入口
- [ ] Toggle 开关可启用/禁用第二密码
- [ ] 笔记编辑器支持 Markdown 预览
- [ ] 笔记不支持插入图片
- [ ] 修改密码功能正常工作

### 批量导出
- [ ] 选中多个文件后可导出
- [ ] 导出的文件是解密后的原始格式
- [ ] 文件名保持不变
- [ ] 使用系统分享表单（可保存到相册/文件）

---

**文档状态**: ✅ 细化完成  
**下一步**: 更新详细实现计划文档
